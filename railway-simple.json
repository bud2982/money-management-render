{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm ci && npm run build",
    "startCommand": "npm start"
  },
  "deploy": {
    "environmentVariables": {
      "NODE_ENV": "production"
    }
  }
}